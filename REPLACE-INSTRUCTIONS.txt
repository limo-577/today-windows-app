Replace these files in your current v1.5.0 source project:

src/TodayApp.jsx
package.json
package-lock.json

No new npm dependencies were added. If you only replace src/TodayApp.jsx, the features work too, but the package version will remain 1.5.0. Replacing package.json and package-lock.json changes the build version to 1.5.1.

Then rebuild:
  npm run build:portable
or
  npm run build:windows
